<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">
    <!--импотрируем файл с зависимостями-->
    <context:property-placeholder location="classpath:musicPlayer.properties"/>
    <!--описание бина-->
    <bean id="musicBean"
          class="misha.JazzMusic">
    </bean>
    <bean id="musicPlayer"
          class="misha.MusicPlayer">
        <property name="music" ref="musicBean"/>  <!--   внедряем зависимость - мы ссылаемся на созданный бин-->
        <!-- определяем поля и значения ссылок на переменные файле проперт ис   -->
        <property name="name" value="${musicPlayer.name}"/>
        <property name="value" value="${musicPlayer.value}"/>
    </bean>


</beans>